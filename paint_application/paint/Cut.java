package ca.utoronto.utm.assignment2.paint;

/**
 * Cut class representing a cut change (done via pressing CTRL + X on the keyboard), keeps track of what to undo and
 * redo.
 */
public class Cut extends Moves{
    /**
     * Undo the last Cut style of change on the canvas.
     */
    @Override
    void undo() {

    }

    /**
     * redo the last undone action that was a cut.
     */
    @Override
    void redo() {

    }
}
